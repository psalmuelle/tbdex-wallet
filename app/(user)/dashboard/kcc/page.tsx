'use client'
import { Layout } from "antd"

const { Content } = Layout

export default function KCC(){
    return (
        <Content className='mt-8 mx-4'>
      This is the KCC page That I want to work on in the mean time
        </Content>
    )
}